source 'https://github.com/CocoaPods/Specs.git'

platform :ios, ‘8.0’
use_frameworks!

target 'MusicBoxer’ do

    pod 'Masonry'
    pod 'DXPopover'
    pod 'Reveal-SDK', :configurations => ['Debug']
end
